@import url('https://fonts.googleapis.com/css?family=Poppins:200i,400&display=swap');

// Imports a font

-------------------------

*, 
*::before, 
*::after {
    box-sizing: border-box; 
}

// Sets everything on box-sizing: border-box so elements don't exceeds the border limits.


body {
    font-family: Poppins, sans-serif;
    font-size: 1rem;
    font-weight: 400;
    line-height: 1.4;
    color: white;
    margin: 0;
}

// This code sets font css styling for the entire body


body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
    z-index: -1;
    background: rgb(58, 58, 158);
    background-image: linear-gradient(
        115deg,
        rgba(93, 93, 199, 0.5),
        rgba(34, 34, 201, 0.4)
      ), url("../images/background.jpeg");
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center;
}

// This code is responsible for setting the background color, image, transparency settings, how the image is displayed, if it repeats, 
// how it is aligned and justified and how much should it cover and how it should be positioned.


.container {
    width: 100%;
    margin: 3.125rem auto 0 auto;
}


@media (min-width: 576px) {
    .container {
      max-width: 540px;
    }
}
  
@media (min-width: 768px) {
    .container {
        max-width: 720px;
    }
}

// This code uses margin x auto x auto to center the container horizontally.
// It uses @media to adapt the container size accordingly to device resolutions.
// The container will occupy the full width that has been assigned to it.


h1 {
    font-weight: 400;
    line-height: 1.15;
}

p {
    font-size: 1.125rem;
}

h1,
p {
    margin: 0;
    margin-bottom: 0.4rem;
}

// This code part is responsible for setting margins, font-size, weight and line height to h1, p elements.

label {
    display: flex;
    align-items: center;
    font-size: 1.125rem;
    margin-bottom: 0.4rem;
}

// This code helps labels position the labels that are used over the form. While also standardizing the font-size accordingly to other elements.

input,
button,
select,
textarea {
  margin: 0;
  font-family: inherit;
  font-size: inherit;
  line-height: inherit;
}

// Standardizing Font elements and margin over other types of elements.

button {
    border: none;
}

// Code below is about Forms

form {
    background-color:rgba(40, 40, 133, 0.87);
    padding: 2.5rem 0.625rem;
    border-radius: 0.25rem;
  }

// This code gives background color to the form, adds padding and rounds the border through border-radius.
  
@media (min-width: 480px) {
    form {
      padding: 2.5rem;
    }
}

// Increases horizontal padding when width is above 480px.

select:invalid {
    color: gray;
}

// This code gives invalid options on select dropdown the gray color.

.input-radio,
.input-checkbox {
    display: inline-block;
    margin-right: 0.625rem;
}

// This helps align elements inside each div as to contain checkbox/radio and text. While also adding a margin to give space from text.


.header {
    margin-bottom: 1.8rem;
}

// Gives a margin at the bottom side to the header.

.text-center {
    text-align: center;
}

// Aligns center, used on header.

.small-text {
    font-size: 0.8rem;
    margin-left: 0.25rem;
    margin-top: 0.25rem;
    color: white;
}

// Text used on parenthesis of certain labels

.description {
    font-style: italic;
    font-weight: 200;
    text-shadow: 2px 2px 2px rgba(48, 46, 46, 0.4);
  }

// Text used on the description on the header

.form-set {
    margin: 0 auto 1rem;
    padding: 0rem;
}

// Sets centered horizontally centered margin for form sets. ( Label and Input )
// Also gives 1rem margin at the bottom.

.form-control {
    display: block;
    width: 100%;
    height: 2.375rem;
    padding: 0.375rem 0.75rem;
    color: #495057;
    background-color: #fff;
    background-clip: padding-box;
    border-radius: 0.25rem;
  }

// Configures the area where you write text on <input text> elements, rounding the border, giving color, background color, 
// background-clip style, height and other things.

.flex-radio {
    display: flex;
    flex-wrap: wrap;
}

// Sets the display style to fill horizontally with wrapping at the end. Used on the input radio form-set so options filled the space horizontally.
// and wrapped at the end of available space.

.input-textarea {
    min-height: 100px;
    width: 100%;
    resize: vertical;
    padding: 0.625rem;
}

// Configures textarea size.